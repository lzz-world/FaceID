<UserControl x:Class="Face.WPF.Views.ConsoleView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:Face.WPF.Views" xmlns:vm="clr-namespace:Face.WPF.ViewModels">
    <UserControl.DataContext>
        <vm:ConsoleViewModel x:Name="ViewModel" />
    </UserControl.DataContext>
    <RichTextBox x:Name="RichTextBox"
                 Background="{StaticResource BackgroupDefualt}"
                 BorderThickness="0"
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"
                 Width="600">
        <FlowDocument x:Name="FlowDocument" />
    </RichTextBox>
</UserControl>

